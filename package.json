{
  "name": "superstar1014-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npx wrangler deploy --dry-run",
    "dev": "npx wrangler dev --local --port 8787",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
